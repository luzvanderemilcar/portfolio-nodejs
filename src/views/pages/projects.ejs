<!DOCTYPE html>
<html lang="fr">
<head>
  <%- include('../partials/head'); %>
  <link rel="stylesheet" href="/css/styles.css" />
</head>

<body>
<header>
  <%- include('../partials/header'); %>
</header>

<main>
   <section>
    <h1 class="primary-text"><%= locals.firstHeading %></h1>
    <div class="description">
      <p>As a javascript developer, I work on some interesting projects. There is an overview of the most relevant.</p>
    </div>
   </section>   
   <hr />
<% for (let projectCategory in locals.projects) { %>
   <section>
	<h2 class="primary-text"><%= locals.projects[projectCategory]?.title %></h2>
	<div class="projects">
		<%  locals.projects[projectCategory]?.items?.forEach(project => {%>
		<div class="subsection" key="<%= project.id %>" class="project">
		<h3><%= project.title %></h3>
		<p><%= project.description %></p>
                <div class="controls">
		<% if (project.codeLink) { %>
			<a href="<%= project.codeLink %>" >Code</a>
		 <% }%>
		</div>
		</div>
		<% }); %>
	</div>
    </section>
<% } %>
</main>

<footer>
  <%- include('../partials/footer'); %>
</footer>

</body>
</html>
